[pytest]
# Run from services/distiller/ (its own invocation - see tests/conftest.py):
#   ../../service/.venv/bin/python -m pytest
testpaths = tests
asyncio_mode = auto
timeout = 60
addopts = -ra --tb=short
