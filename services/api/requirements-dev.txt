# Test-only dependencies (not shipped in the service image).
# Install: .venv/bin/python -m pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-timeout==2.4.0

ruff==0.14.2                    # lint; config in ruff.toml at the repo root
